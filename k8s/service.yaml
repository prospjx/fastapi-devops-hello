apiVersion: v1
kind: Service
metadata:
  name: fastapi-hello
  labels:
    app: fastapi-hello
spec:
  selector:
    app: fastapi-hello
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP